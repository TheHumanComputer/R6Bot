let Discord = module.require("discord.js")
const botsettings = require("../bot_settings.json")
module.exports.run = async (bot, message, args) => {
	message.reply("sorry that command is under work!")
}

module.exports.help = {
    name: "guildMember"
}