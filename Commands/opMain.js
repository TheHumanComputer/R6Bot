const { Client, Attachment } = require('discord.js');
const fs = require('fs');

module.exports.run = async (bot, message, args) => {
        message.reply("Still working on that command! Please stand by!");
}

module.exports.help = {
    name: "mainOp"
}